#include <iostream>
#include <cstdio>

using namespace std;

int main()
{
	int d;
	long ld;
	char c;
	float f;
	double lf;
	
	scanf("%d %ld %c %f %lf", &d, &ld, &c, &f, &lf);
	cout << d << endl << ld << endl << c << endl << f << endl << lf;
	return (0);
}
